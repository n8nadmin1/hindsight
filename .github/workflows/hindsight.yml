name: Deploy HindSight Service
on:
  workflow_dispatch: 
jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    - name: First Workflow
      run: echo "Welcome, ADEO!"
